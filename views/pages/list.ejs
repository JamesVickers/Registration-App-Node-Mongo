
<!DOCTYPE html>
<html lang="en"> 

    <head>
        <% include ../partials/head %>
    </head>

    <body>

        <header>
            <% include ../partials/header %>
        </header>
            
        <main>
            <h2>Display register here...</h2>

            
            <ul>
                <% for(var i=0; i<registers.length; i++) {%>
                  <li>
                    <span><%= registers[i].firstName %></span>
                    <span><%= registers[i].lastName %></span>
                    <span><%= registers[i].entryTime %></span>
                  </li>
                <% } %>
                </ul>
            
       
        </main>
        
        <footer>
            <% include ../partials/footer %>
        </footer>

    </body>

</html>
